// 将设置放入此文件中以覆盖默认值和用户设置。
{
  "files.exclude": {
    "USE_GITIGNORE": true,
    "**/*.js": {
      "when": "$(basename).ts"
    },
    "**/*.map": true,
    "run": true,
    "logs": true,
    "out": true,
    "node_modules": true
  }
}